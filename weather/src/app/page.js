import { useState } from "react";
import FetchButton from "./components/atoms/button/FetchButton";

export default function Home() {
// to do
  //
  // - button that fetches
  // - container for button
  // - container for display content (empty, loading, fulfilled states)
  // - fetch content (handle and format content)
  // - error handling
  // - styling
  // - breakpoints (mobile-first methodology)
  // - function to clear data


  return (
    <>
    <FetchButton />
    </>
  );
}
